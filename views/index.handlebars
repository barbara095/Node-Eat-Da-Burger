{{!-- <h1>Eat Da Burger!</h1>

<img src="public/assets/img/burger.png" alt="burger"></img>

<h2>Choose your burger</h2>

<ul>
  {{#each burgers}}
      <li>{{}}</li>
  {{/each}}
</ul>

<form class="create-form">

    <div class="form-group">
    <label for="ca">Burger:</label>
    <input type="text" id="burg" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Devoured</label><br>
    <input type="radio" name="devour" value="1" checked> Devoured!<br>
    <input type="radio" name="waiting" value="0"> Waiting...
  </div>

  <button type="submit">Feed me this burger!</button>
  <button type="submit">I don't want this burger</button>

</form> --}}


<div class="jumbotron">
      <div class="container">
        <div class="row">
          <div class="col-xs-6 col-sm-6 p-0 burger-title">
            <h1>EAT</h1>
            <h3>DA</h3>
            <h1>BURGER!</h1>
          </div>
          <div class="col-xs-6 col-sm-6">
          <img src="public/assets/img/burger.png" />
        </div>
        <form class="create-form">
          <div class="form-row align-items-center">
            <div class="col-auto">
              <label class="sr-only" for="inlineFormInput">Burger</label>
              <input type="text" class="form-control mb-5" id="inlineFormInput" placeholder="Add a burger to the menu...">
            </div>
            <div class="col-auto">
              <button type="submit" class="btn btn-success ml-5 mb-5">Add!</button>
            </div>
          </div>
        </form>
      </div>
    </div>
    </div>
    <div class="container">
      <div class="row">
        <div class="col-xs-6 col-sm-6">
          <div class="panel panel-success">
            <div class="panel-heading">
              <h4 class="panel-title">BURGERS</h4>
          </div>
          <div class="panel-body">
          <ul class="d-inline">
            {{#each burgers}}
            {{#unless devoured}}
            <li>{{this.burger_name}} <button class="btn btn-warning change-eaten" data-id="{{id}}" data-devoured="{{devoured}}">EAT!</button></li>
            <hr>
            {{/unless}}
            {{/each}}
          </ul>
        </div>
        </div>
        </div>
        <div class="col-xs-6 col-sm-6">
          <div class="panel panel-success">
            <div class="panel-heading">
              <h4 class="panel-title">DEVOURED</h4>
          </div>
          <div class="panel-body">
          <ul class="d-inline">
            {{#each burgers}}
            {{#if devoured}}
            <li>{{this.burger_name}} <button class="btn btn-danger delete-burger" data-id="{{id}}"><i class="fa fa-trash"></i></button></li>
            <hr>
            {{/if}}
            {{/each}}
          </ul>
        </div>
        </div>
      </div>
      </div>
    </div>